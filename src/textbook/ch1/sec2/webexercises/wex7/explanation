sqrt(2) is an irrational number, which has mathematically infinite digits, but the computer can only store a finite
number of digits, so Math.sqrt(2) * Math.sqrt(2) will yield something extremely close to 2.0 but not quite 2, so
Math.sqrt(2) * Math.sqrt(2) == 2 is false.